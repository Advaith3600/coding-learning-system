## Local interactive code judge (Next.js + Judge0 CE)

### Startup

1. Build and start everything:

```bash
docker compose up --build
```

2. On first run, wait ~30 seconds for Judge0 to initialize its database.
3. Open `http://localhost:3000`

### Useful endpoints

- Judge0 system info: `http://localhost:2358/system_info`

